Data was gathered via a custom Node.js scraper. To respect the source's bandwidth, 
the script implements a 1-second delay between requests and localizes all assets 
to prevent unnecessary server load on the community database.